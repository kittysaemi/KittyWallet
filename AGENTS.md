# 미냥이 지갑 (KITTYWALLET) AI 작업 규칙

## 프로젝트 개요

미냥이 지갑은 PWA 기반 모바일 가계부 서비스이다.

본 프로젝트는 문서 중심 개발(Document Driven Development)을 원칙으로 하며, 모든 구현은 정의된 문서를 기준으로 수행한다.

---

# 최우선 원칙

문서 > 코드 > 추정

정의되지 않은 기능은 임의 구현하지 않는다.

정책이 없는 경우 반드시 문서를 확인하거나 문서 작성 필요성을 먼저 제안한다.

작업 시작 전 반드시 `docs/00_project/문서인덱스.md`를 확인하여 관련 문서를 식별한다.

---

# 작업 시작 순서

작업 전 문서 참조 순서는 `docs/00_project/문서인덱스.md`의 최상위 기준과 작업 유형별 빠른 참조 경로를 따른다.

본 문서는 최상위 AI 작업 규칙이며, 상세 문서 탐색 순서는 문서인덱스에서 관리한다.

## GitHub Issue 기반 개발 절차

GitHub Issue를 처리하는 작업은 항상 다음 순서를 따른다.

1. GitHub에서 대상 Issue 본문, 상태, 라벨, 댓글, 서브이슈, 선행/의존 Issue 상태를 확인한다.
2. 선행 Issue가 open 상태이거나 완료되지 않은 경우 구현을 시작하지 않고 작업을 종료한 뒤 사용자에게 알려준다.
3. 서브이슈가 있으면 서브이슈를 먼저 검토한다.
4. 서브이슈가 개발 범위이고 현재 작업에서 함께 처리 가능하면 서브이슈까지 구현, 검증, PR 연결 대상에 포함한다.
5. 서브이슈가 독립적으로 먼저 완료되어야 하는 범위이면 해당 서브이슈 작업을 우선 진행하고 상위 Issue 작업은 보류한다.
6. 개발 시작 전 반드시 새 작업 브랜치를 생성한다.
7. 로컬에서 구현한 뒤 lint, type-check, test, build, 필요한 Docker/health 검증까지 성공해야 커밋한다.
8. 검증 성공 후에만 커밋, push, PR 생성을 진행한다.
9. PR 본문에는 상위 Issue와 처리한 서브이슈를 모두 연결한다.
10. 작업자가 완료한 작업 범위, 완료 조건, 서브이슈 체크박스는 검증과 PR 생성 결과를 기준으로 GitHub Issue 본문에서 갱신한다.

Issue 완료 여부, 선행 관계, 서브이슈 상태는 로컬 추정이나 문서가 아니라 항상 GitHub Issue 상태를 기준으로 판단한다.

각 단계로 넘어가기 전에는 해당 단계와 관련된 문서에 변경 사항이 있는지, 추가로 지켜야 할 기준이 있는지 확인한다. 개발, 테스트, Docker, Git, PR, 이슈 갱신 등 모든 단계는 관련 문서 확인 후 진행한다.

---

# 개발 원칙

## 문서 우선

* 구현 전 관련 문서 확인
* 정책 변경 시 문서 먼저 수정
* API 변경 시 API 문서 먼저 수정
* 화면 변경 시 화면정의 문서 먼저 수정

---

## 구조 우선

기능 구현보다 구조 일관성을 우선한다.

허용 의존성

pages → features → entities → shared

역방향 의존성 금지

---

## 상태 관리

Server State

* TanStack Query 사용

Local State

* Zustand 사용

Form State

* 전역 상태 사용 금지

---

## API 처리

API 직접 호출 금지

반드시 entities/api 계층 사용

UI 컴포넌트 내부 fetch 사용 금지

---

## 오프라인 정책

Offline First 원칙 준수

오프라인 저장 시 Sync Queue 사용

동기화 정책을 우회하는 구현 금지

---

## UI 원칙

모바일 우선 설계

반드시 고려

* Safe Area
* 모바일 키보드
* 터치 영역
* 하단 네비게이션
* PWA 환경

---

## Git 규칙

Git 작업은 Git작업규칙.md를 따른다.

---

## 문서 규칙

문서 작업은 문서관리규칙.md를 따른다.

---

## 금지 사항

* 문서에 없는 기능 임의 추가
* API 규격 임의 변경
* 상태 구조 임의 변경
* 직접 DB 접근 가정
* 하드코딩된 비즈니스 규칙 추가

---

# 최종 목표

* 유지보수 가능한 구조
* 기능 확장 시 최소 수정
* API 변경 영향 최소화
* 모바일/PWA 최적화
* AI 협업 가능한 프로젝트 유지
